{
  "buildCommand": "cd client && npm install && npm run build:client",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --prefix client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 